/**
 * @file Declares AccountType enumeration representing a user's account type
 */

/**
 * Enum for a user's account type
 * @readonly
 * @enum {string}
 */
enum AccountType {
    Personal = 'PERSONAL',
    Academic = 'ACADEMIC',
    Professional = 'PROFESSIONAL'
};
export default AccountType;
